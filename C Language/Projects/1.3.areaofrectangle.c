#include<stdio.h>
int main()
{
    float length,breadth;
    printf("enter length : ");
    scanf("%f", &length);
    printf("enter breadth : ");
    scanf("%f", &breadth);
    printf("Area of the rectangle is : %f", length*breadth);
    return 0;
}